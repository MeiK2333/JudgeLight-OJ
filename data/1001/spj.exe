#!/usr/bin/env python
import sys

with open('output.txt') as fr:
    output_data = fr.read()

with open('data.out') as fr:
    data = fr.read()

if output_data.lower().strip() == data.lower().strip():
    sys.exit(0)
else:
    sys.exit(1)
